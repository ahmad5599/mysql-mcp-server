{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "npm",
            "script": "watch",
            "group": "build",
            "presentation": {
                "reveal": "never"
            },
            "problemMatcher": "$tsc-watch",
            "options": {
                "cwd": "${workspaceFolder}/extension"
            }
        }
    ]
}